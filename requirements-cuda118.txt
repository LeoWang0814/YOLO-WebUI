# YOLO-WebUI - NVIDIA CUDA installation.
#
# This file intentionally does not set a package index. Configure the PyTorch
# CUDA 11.8 index or a compatible mirror before installing if your source
# requires a CUDA-specific wheel. A preinstalled 2.0.1+cu118 wheel satisfies
# the public 2.0.1 version constraint below.
torch==2.0.1
torchvision==0.15.2

-r requirements-base.txt
