# YOLO-WebUI - CPU installation (default)
#
# This file intentionally does not set a package index. Pip uses the user's
# configured index or mirror. For an NVIDIA CUDA environment, use
# requirements-cuda118.txt instead.
torch==2.0.1
torchvision==0.15.2

-r requirements-base.txt
